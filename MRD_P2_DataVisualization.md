# LottoMonitor P2 数据可视化增强需求文档 (MRD)

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0.0 |
| 生效日期 | 2026-03-10 |
| 需求优先级 | P2 (中优先级) |
| 产品负责人 | 产品经理 |
| 状态 | 已实现 |

---

## 1. 背景与目标

### 1.1 背景

当前 StatsView 已提供基础的统计分析功能（和值走势、极差走势、位置热号、后区热力图）。为提升产品的数据分析深度，满足进阶彩民对号码规律探索的需求，需要增加遗漏值分析和连号/重号分析功能。

### 1.2 目标

- **用户价值**：帮助用户识别长期未出现的冷号和近期活跃的热号
- **分析深度**：通过遗漏值和连号/重号分析，提供更全面的号码规律视角
- **产品竞争力**：与市面上基础的走势图工具形成差异化

---

## 2. 需求范围

### 2.1 功能需求清单

| 需求 ID | 功能模块 | 需求描述 | 优先级 |
|---------|---------|---------|--------|
| VIS-001 | 遗漏值分析 | 统计前区(1-35)和后区(1-12)每个号码的遗漏期数（多久没出） | P2 |
| VIS-002 | 遗漏值可视化 | 以颜色深浅展示遗漏值，支持按遗漏期数排序 | P2 |
| VIS-003 | 连号分析 | 统计历史开奖中出现连续号码(如05,06)的频率 | P2 |
| VIS-004 | 重号分析 | 统计与上一期重复号码的数量分布 | P2 |
| VIS-005 | 区间筛选 | 以上统计均支持按时间范围筛选 | P2 |

### 2.2 非功能需求

- 与现有 UI 风格保持一致（Tailwind CSS）
- 移动端优先，适配小屏幕显示
- 计算性能优化，大数据量不卡顿

---

## 3. 功能详细设计

### 3.1 VIS-001/VIS-002: 遗漏值分析

#### 3.1.1 计算逻辑

```typescript
// 遗漏值定义：从最近一期往前数，该号码连续未出现的期数
// 示例：号码 05 在最近 10 期都没出现，则遗漏值 = 10

interface OmitStats {
  num: number;        // 号码
  omit: number;       // 遗漏期数
  lastDrawId: string; // 最近出现期号
  lastDrawDate: string; // 最近出现日期
}
```

#### 3.1.2 展示形式

**前区遗漏值矩阵 (1-35)**
- 布局：5行 x 7列网格
- 每个格子显示：号码 + 遗漏值
- 颜色规则：
  - 遗漏 0-5 期：绿色系（热号）`bg-green-50 text-green-600`
  - 遗漏 6-15 期：黄色系（温号）`bg-yellow-50 text-yellow-600`
  - 遗漏 16-30 期：橙色系（冷号）`bg-orange-50 text-orange-600`
  - 遗漏 >30 期：红色系（极冷）`bg-red-50 text-red-600`

**后区遗漏值矩阵 (1-12)**
- 布局：2行 x 6列网格
- 颜色规则与前区一致

#### 3.1.3 排序功能
- 默认：按号码从小到大
- 可选：按遗漏值从大到小（快速识别冷号）

### 3.2 VIS-003: 连号分析

#### 3.2.1 连号定义

**前区连号**：前区5个号码中，存在相邻的两个或更多号码
- 示例：`[05, 06, 12, 23, 34]` 包含连号 `05-06`
- 示例：`[11, 12, 13, 25, 30]` 包含三连号 `11-12-13`

**统计维度**：
1. 连号出现频率：有连号的期数 / 总期数
2. 连号长度分布：2连号、3连号、4连号、5连号的出现次数
3. 连号位置偏好：连号出现在前区小号区(1-12)/中号区(13-24)/大号区(25-35)的分布

#### 3.2.2 展示形式

**连号统计卡片**
```
┌─────────────────────────────────────┐
│ 连号统计                出现率: 62%  │
├─────────────────────────────────────┤
│ 二连号 ████████████ 45次           │
│ 三连号 ██████ 18次                 │
│ 四连号 ██ 3次                      │
│ 五连号 □ 0次                       │
├─────────────────────────────────────┤
│ 位置分布:                           │
│ 小号区(1-12)  ████████ 28次        │
│ 中号区(13-24) ████████████ 35次    │
│ 大号区(25-35) ████ 12次            │
└─────────────────────────────────────┘
```

### 3.3 VIS-004: 重号分析

#### 3.3.1 重号定义

与上一期开奖号码重复的号码数量。

**前区重号**：本期前区与上一期前区重复的号码数（0-5个）
**后区重号**：本期后区与上一期后区重复的号码数（0-2个）

#### 3.3.2 展示形式

**重号分布统计**
```
┌─────────────────────────────────────┐
│ 前区重号分布                        │
├─────────────────────────────────────┤
│ 0个重号 ██████████ 35%             │
│ 1个重号 ████████████████ 42%       │
│ 2个重号 ████████ 18%               │
│ 3个重号 ██ 4%                      │
│ 4个重号 □ 1%                       │
│ 5个重号 □ 0%                       │
└─────────────────────────────────────┘
```

**最近重号走势**
- 折线图展示最近 30 期的重号数量变化

---

## 4. UI/UX 设计规范

### 4.1 新增统计模块布局

在现有 StatsView 底部新增两个模块：

```
[和值趋势图]
[极差走势]
[位置热号]
[后区热力图]
[遗漏值分析]      <-- 新增
[连号/重号分析]    <-- 新增
```

### 4.2 遗漏值颜色规范

| 遗漏期数 | 背景色 | 文字色 | 含义 |
|---------|--------|--------|------|
| 0-5 | bg-green-50 | text-green-600 | 热号 |
| 6-15 | bg-yellow-50 | text-yellow-600 | 温号 |
| 16-30 | bg-orange-50 | text-orange-600 | 冷号 |
| >30 | bg-red-50 | text-red-600 | 极冷 |

### 4.3 响应式适配

- 移动端：单列布局，横向滚动支持
- 桌面端：双列并排显示

---

## 5. 技术实现方案

### 5.1 新增工具函数 (utils.ts)

```typescript
// 计算遗漏值
export const calculateOmitStats = (
  history: LottoDraw[], 
  isFront: boolean
): OmitStats[] => { ... }

// 计算连号统计
export const calculateConsecutiveStats = (
  history: LottoDraw[]
): ConsecutiveStats => { ... }

// 计算重号统计
export const calculateRepeatStats = (
  history: LottoDraw[]
): RepeatStats => { ... }
```

### 5.2 修改文件

- `utils.ts` - 添加统计计算函数
- `components/StatsView.tsx` - 添加可视化模块

---

## 6. 验收标准

### 6.1 功能验收

| 验收项 | 验收标准 |
|--------|---------|
| 遗漏值计算 | 每个号码的遗漏值计算准确，与历史数据核对无误 |
| 颜色分级 | 遗漏值按规则正确显示对应颜色 |
| 排序功能 | 点击排序按钮后，号码按遗漏值正确排序 |
| 连号识别 | 正确识别二连号、三连号等，不遗漏不误判 |
| 重号统计 | 重号数量统计准确，分布图表数据正确 |
| 范围筛选 | 切换时间范围后，统计数据实时更新 |

### 6.2 性能验收

- 2500期数据计算遗漏值 < 100ms
- 页面渲染无明显卡顿

---

## 7. 附录

### 7.1 相关文件

- 实现涉及文件：
  - `utils.ts` - 统计计算逻辑
  - `components/StatsView.tsx` - 可视化展示

### 7.2 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0.0 | 2026-03-10 | 初始版本 | - |

